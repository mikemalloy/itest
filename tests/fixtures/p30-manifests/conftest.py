"""The stub files under here are fixture *data*: a P30-era suite, copied into a
temporary checkout by the migration tests. They are never collected in place."""

collect_ignore_glob = ["*/itest_tests/*.py"]
